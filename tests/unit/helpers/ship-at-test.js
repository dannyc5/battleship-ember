import { shipAt } from '../../../helpers/ship-at';
import { module, test } from 'qunit';

module('Unit | Helper | ship at');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = shipAt(42);
  assert.ok(result);
});
