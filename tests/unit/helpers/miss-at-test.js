import { missAt } from '../../../helpers/miss-at';
import { module, test } from 'qunit';

module('Unit | Helper | miss at');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = missAt(42);
  assert.ok(result);
});
