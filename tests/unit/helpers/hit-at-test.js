import { hitAt } from '../../../helpers/hit-at';
import { module, test } from 'qunit';

module('Unit | Helper | hit at');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hitAt(42);
  assert.ok(result);
});
