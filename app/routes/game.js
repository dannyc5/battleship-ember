import Ember from 'ember';

export default Ember.Route.extend({
  // afterModel(model, transition) {
  //   let human = model.get('human').then((human) => {
  //         debugger
  //     this.store.find('board', human.get('board_id')).then((board) => {
  //         debugger
  //       if (!model.battleshipOn()) {
  //         debugger
  //         // this.transitionTo('game.new_human', model);
  //       }
  //     })
  //   })
  // }
});
